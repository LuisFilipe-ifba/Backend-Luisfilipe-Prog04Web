spring.application.name=empreendimentos

#spring.datasource.url=jdbc:postgresql://db.hfntskpezergrtwzoxfj.supabase.co:5432/postgres
#spring.datasource.username=postgres
#spring.datasource.password=ja3XFWHvwQ31dU2Y
spring.datasource.driver-class-name=org.postgresql.Driver

# Cria/atualiza as tabelas automaticamente com base nas @Entity.
# "update" é bom para desenvolvimento; NUNCA use em produção (troque para "validate" ou "none").
spring.jpa.hibernate.ddl-auto=update

# Mostra no console o SQL que o Hibernate está executando -- essencial para depurar agora.
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.open-in-view=false

jwt.secret=9VXZ8ZJEKYGWBFMO5C0XKT0FT63580TF
jwt.expiration-minutes=60

spring.datasource.url=${SPRING_DATASOURCE_URL}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD}
server.port=${PORT:8080}